{
  "name": "greeter",
  "version": "1.0.0",
  "license": "(MIT AND Apache-2.0)",
  "scripts": {
    "build": "near-sdk build",
    "deploy": "npm run build&& rm -rf neardev && near js dev-deploy --base64File build/contract.base64 --deposit 1 && export $(cat ./neardev/dev-account.env) && near js call $CONTRACT_NAME init --accountId $CONTRACT_NAME --deposit 1 && echo $CONTRACT_NAME"
  },
  "dependencies": {
    "near-sdk-js": "0.3.0"
  },
  "resolutions": {
    "@babel/preset-env": "7.13.8"
  }
}
